spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SergioSSJ/email-confirmation-properties
          search-paths:
            - "ms-config-properties/{application}/{profile}"